The keyword used to describe a variable is let.  
No, you can not declare a variable multiple times.
camelCase is used when the name contains multiple words.
To declare an unchanging variable we use const with stands for constant
The difference + and ++ is the + is an addition operator and ++ is an incrementing operator.
I would like to know more about the remainer % to learn more about how JS operates in the console, and how they work together.
A string is the data typed surrounded by quotes
There are 3 types of quotes in JS. The double and single quotes which there is no difference between them and the backticks which allows us to embed variables and expressions into a string by wrapping them in ${}.
A boolean is the data type has only two values.
The null value represents nothing, empty or value unknown.  
The typeof operator is useful when we want to process the values of different types differently.
Just like a cabinet, you can add and remove things, in an object, as needed.
The properties are name and the values are John and 30.
A breakpoint is a point of code where the debugger will automatically pause the JS execution.
To set a breakpoint insert the debugger where you want to pause the JS exection in the source tab of the dev tools in the browser.
